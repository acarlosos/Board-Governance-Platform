# Resultado — Fase 19A.9 (Docs finais pós-19A/19B)

Relatório de [`19A.9-docs-final.md`](./19A.9-docs-final.md). Data: 2026-05-11.

## 1. Diff doc vs código — inconsistências corrigidas

| # | Divergência | Acção |
|---|-------------|--------|
| 1 | Roadmap interno listava **19A.8** como «em curso» com referência à secção QA sem sufixo **(arquivado)** | Texto actualizado para evidência estática + **GO staging pendente** + link ao `19A.8-staging-validation.result.md`. |
| 2 | **19A.9** sem estado «concluída» no bloco roadmap da ficha | Marcada **concluída** com link ao presente result. |
| 3 | Bloco bash e tabela GO/NO-GO com contagens fixas **286/286** e assertions desactualizadas | Substituído por referência dinâmica ao result **19A.9** ou CI. |
| 4 | Tabela de cobertura com **352/352** fixo (envelhece rápido) | Alinhada a «última execução documentada» no result 19A.9. |
| 5 | `19A.9-docs-final.result.md` anterior citava **351/351** na tabela de secções | Corrigido; contagens finais nesta execução: **352/352**. |
| 6 | **D10** em `architecture.md` apontava remoção legacy para **19B.5** em vez de **19B.6** | Corrigido na tabela D1–D10. |

## 2. Secções alteradas (âncora / linhas)

| Ficheiro | Secção / linhas (aprox.) |
|----------|-------------------------|
| `docs/architecture.md` | Tabela D1–D10: **D10** corrigido (`remoção legacy` → **19B.6**, não 19B.5). |
| `docs/roadmap.md` | Fase 19B: bullets **19B.5** (rascunho/condicional + link spec) e **19B.6** (pendente). |
| `docs/features/dashboard.md` | `### Roadmap 19A → 19B` — bullets **19A.8**, **19A.9** (L310–L311); bloco **Validação local** bash (L458); tabela **Cobertura automática** linha regressão suite (L473); `#### Critérios GO / NO-GO produção` — linha suite (L566). |
| `docs/features/README.md` | *(sem alteração — índice cobre as 18 fichas em `docs/features/`)* |

## 3. Duplicações eliminadas (architecture ↔ features)

- **D10 (architecture):** texto da tabela D1–D10 contradizia o roadmap da ficha (`19B.6` remoção legacy vs «19B.5»); corrigido para **19B.6** apenas na linha D10 — sem parágrafo longo duplicado da ficha.

## 4. Links smoke (`rg '\]\([^)]*\.md' docs/`)

- Amostragem nos ficheiros tocados + índice `docs/execution/README.md`: destinos `.md` relativos existem no repositório.
- **Nota:** não foi revalidado cada URL externa ou âncora `#` dentro dos mesmos ficheiros (fora do escopo smoke da spec).

## 5. Validação §11 roadmap vs D33

- A spec §3 pede **19A.8 → ✅ após GO de 19A.8**; o **§11** checklist diz «19A.8/19A.9/19B.3 = ✅».
- **Decisão documental (D33):** findings 19A.8 **não** se duplicam no roadmap; o **GO staging** ainda **não** foi obtido — `roadmap.md` mantém **19A.8 pendente staging**. **19A.9** e **19B.3** estão **✅**. Isto não é bloqueio arquitectural: é estado honesto até QA staging.

## 6. Testes

- `php artisan test` — **352/352** passed, **2481** assertions (~14,2 s).

## 7. Bloqueios

- Nenhum (apenas doc sync + suite).
