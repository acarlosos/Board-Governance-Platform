# Fase 19A.9 — Documentação final pós-19A/19B.1-3

> Spec **documental** (sem código). Sincroniza `features/dashboard.md`, `architecture.md` e `roadmap.md` com o estado real após 19A.7, 19A.8, 19B.1, 19B.2 e 19B.3.

## 1. Contexto

Após o ciclo completo de 19A + 19B.1-3, a ficha `docs/features/dashboard.md` cresceu organicamente. Algumas secções podem ter duplicações com `docs/architecture.md` ou referências a estados intermédios que já não correspondem ao código actual. Esta fase é uma passagem de pente fino — sem alteração de comportamento, apenas docs coerentes com código.

## 2. Decisões finais confirmadas

| ID | Decisão |
|---|---|
| **D31** | Docs **espelham código actual**. Sem marketing, sem tutorial Laravel/Filament genérico. Manter linguagem objectiva. |
| **D32** | Eliminar duplicações entre `architecture.md` (visão global) e `features/dashboard.md` (detalhe módulo). `architecture.md` aponta para a ficha; ficha não repete arquitectura macro. |
| **D33** | Findings de QA 19A.8 ficam **arquivados** numa subsecção `## QA 19A.8 — findings (arquivado)` da ficha; **não** se duplicam no roadmap nem em architecture. |

## 3. Escopo

- Rever `docs/features/dashboard.md` (foco nas secções 19A.0–19A.9 + 19B.1–19B.3).
- Rever `docs/architecture.md` → secção "Executive Dashboard Read Architecture" (eliminar redundâncias com a ficha; manter apenas tabela arquitectural + D1-D10 + pendência projection model).
- Actualizar `docs/roadmap.md`:
  - Fase 19A.8 → ✅ (após GO de 19A.8)
  - Fase 19A.9 → ✅ (após esta fase)
  - Fase 19B.3 → confirmar ✅ (já marcada pelo Executor)
  - Refletir status de 19B.4 / 19B.5 / 19B.6 conforme calendário.
- Confirmar links internos funcionam (`grep -r '](.*\.md' docs/` smoke).

## 4. Fora do escopo

- Refactor de código.
- Novas decisões arquitecturais (qualquer `Dxx` novo).
- Alterações em `.cursor/rules/*.mdc` (excepto eventual sync de referência a docs).
- Re-edição de specs/results de fases já fechadas (19A.* / 19B.1 / 19B.2 / 19B.3) — esses são histórico imutável.

## 5. Arquivos esperados

- Alterar: `docs/features/dashboard.md`, `docs/architecture.md`, `docs/roadmap.md`, `docs/features/README.md` (se faltar entrada).
- Criar: `docs/execution/19A.9-docs-final.result.md`.

## 6. Implementação passo a passo

1. **Diff doc vs código** — passar pela ficha `features/dashboard.md` secção 19A/19B e marcar 5 pontos críticos onde texto diverge do código (ex.: nome de classe antiga, chave de cache obsoleta, número de testes desactualizado, referência a TTL antigo, referência a fase ainda como "em curso" depois de fechada).
2. **Reescrever subsecções desactualizadas** — usar nomes/classes/chaves actuais. Substituir contagens fixas ("X testes") por intervalo qualitativo ou link para `docs/execution/{fase}.result.md`.
3. **Arquivar findings 19A.8** — mover subsecção "QA 19A.8 — findings" para "QA 19A.8 — findings (arquivado)" e adicionar nota inicial: "Esta secção é histórico congelado pós-GO produção; ver `docs/execution/19A.8-staging-validation.result.md` para o relatório oficial."
4. **Actualizar `roadmap.md` + índice de features** — marcar 19A.8 e 19A.9 como concluídas; confirmar `docs/features/README.md` lista todas as fichas existentes.
5. **Smoke links** — `rg '\]\([^)]*\.md' docs/` e abrir cada link único. Reportar e corrigir broken links no result.

## 7. Regras de segurança / tenancy

Não aplicável — alteração só docs. Mas qualquer exemplo de código nas docs deve respeitar tenancy (sem `find()` sem scope, sem `tenant_id` em log cru).

## 8. Estratégia de chaves / IDs / nomes

Manter chaves canónicas referenciadas:
- L1 KPI: `dashboard_metrics:v1:{cacheSegment}:{period}`
- L2 shared: `dashboard_snapshot:v1:{cacheSegment}:{period}:shared:plain`
- Observability prefix: `dashboard:obs:`
- Projection table: `tenant_dashboard_snapshots`
- Flags: `BGP_DASHBOARD_USE_EXECUTIVE_WIDGETS`, `BGP_DASHBOARD_USE_PROJECTION`

## 9. Mapas obrigatórios

Tabela docs → responsabilidade após esta fase:

| Documento | Conteúdo após 19A.9 |
|---|---|
| `docs/roadmap.md` | índice cronológico + estado das fases |
| `docs/architecture.md` | visão técnica global; **não** repete detalhes da ficha |
| `docs/features/dashboard.md` | detalhe completo do módulo; histórico QA arquivado |
| `docs/execution/{fase}.md` + `.result.md` | blueprint + relatório técnico de cada fase |
| `.cursor/rules/dashboard.mdc` | regras vivas (não duplica spec) |

## 10. Testes obrigatórios

Nenhum teste de código novo. **Suite continua verde** (`php artisan test` na conclusão).

## 11. Validação final

- Nenhum link interno quebrado.
- Nenhuma secção em `features/dashboard.md` faz referência a estado "em curso" para fase já fechada.
- `architecture.md` não duplica > 3 linhas seguidas de texto presente em `features/dashboard.md`.
- `roadmap.md` reflecte 19A.8/19A.9/19B.3 = ✅.
- Suite verde.

## 12. Entregáveis esperados

1. Alterações nos 4 ficheiros acima.
2. `docs/execution/19A.9-docs-final.result.md` com:
   - lista exacta de secções alteradas (com linhas-âncora);
   - lista de duplicações eliminadas (architecture ↔ features);
   - confirmação de links smoke OK;
   - confirmação suite verde.

## 13. Commit sugerido

```
docs(dashboard): sync features/architecture/roadmap pós-19A.8/19B.1-3 (19A.9)
```

## 14. Instrução final para o Executor

> Não introduzir decisões novas. Se durante a sincronização detectar inconsistência arquitectural (não meramente textual), **PARAR e devolver ao Arquitecto** — não é trabalho desta fase tomar decisão técnica nova. Preencher result com a inconsistência detectada e bloqueio.
