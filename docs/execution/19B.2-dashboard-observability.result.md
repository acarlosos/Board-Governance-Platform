# Resultado — Fase 19B.2 (observabilidade leve do cache executivo)

Documento de fecho da execução de [`19B.2-dashboard-observability.md`](./19B.2-dashboard-observability.md). Data de validação: 2026-05-11.

## 1. Resumo executivo

Foram introduzidos counters diários agregados (`ExecutiveDashboardObservability`) com chaves `dashboard:obs:*:{Y-m-d}` (TTL 7 dias), instrumentação em três pontos (`DashboardMetricsService::getMetrics`, `ExecutiveDashboardReadService::loadOrComputeShared` com skip de `cacheSegment === 'none'`, `ExecutiveDashboardCacheInvalidator` com **uma** `recordInvalidation()` por `invalidateForTenant`), e o comando Artisan `dashboard:cache-stats` (`--day=`, `--json`). Sem APM externo, sem métricas por tenant (D15), sem alteração de TTL ou comportamento de cache além da contagem.

## 2. Ficheiros criados ou alterados

**Novos**

- `app/Services/Dashboard/Executive/Observability/ExecutiveDashboardObservability.php`
- `app/Console/Commands/Dashboard/CacheStatsCommand.php`
- `tests/Unit/Dashboard/Executive/Observability/ExecutiveDashboardObservabilityTest.php`
- `tests/Feature/Dashboard/Executive/Observability/L1L2InstrumentationTest.php`
- `tests/Feature/Dashboard/Executive/Observability/InvalidatorInstrumentationTest.php`
- `tests/Feature/Dashboard/Executive/Observability/CacheStatsCommandTest.php`
- `docs/execution/19B.2-dashboard-observability.result.md` (este ficheiro)

**Modificados**

- `app/Services/Dashboard/DashboardMetricsService.php`
- `app/Services/Dashboard/Executive/ExecutiveDashboardReadService.php`
- `app/Services/Dashboard/Executive/Cache/ExecutiveDashboardCacheInvalidator.php`
- `tests/Unit/Dashboard/Executive/ExecutiveDashboardReadServiceCompositionTest.php`
- `.cursor/rules/dashboard.mdc`, `.cursor/rules/cache.mdc`
- `docs/features/dashboard.md`, `docs/roadmap.md`, `docs/architecture.md`, `docs/execution/README.md`

## 3. Resultado da suite de testes (completa)

Comando: `php artisan test`.

```json
{
  "tool": "phpunit",
  "result": "passed",
  "tests": 328,
  "passed": 328,
  "assertions": 2237,
  "duration_ms": 12706
}
```

Filtros §11: `Observability`, `Dashboard`, `Api` — verdes. `php artisan filament:assets` — OK.

## 4. Smoke `dashboard:cache-stats --json`

```json
{
 "day": "2026-05-12",
 "l1": {
 "hits": 0,
 "misses": 0,
 "hit_ratio": 0
 },
 "l2": {
 "hits": 0,
 "misses": 0,
 "hit_ratio": 0
 },
 "invalidations": 0
}
```

## 5. Sentinela anti-leak D15 (`rg`-equivalente em `app/` + `tests/`)

Padrão `dashboard:obs:.*(t_|user_|@)` sobre `app/` e `tests/`: **0 ocorrências** (apenas a spec em `docs/execution/` menciona exemplos negativos).

## 6. D13–D18 (confirmação)

Registados em `.cursor/rules/dashboard.mdc` (secção **Observabilidade (19B.2)**) e na ficha `docs/features/dashboard.md` (secção **19B.2**). `cache.mdc` referencia o prefixo `dashboard:obs:` e a proibição D15.

## 7. Reordenação roadmap 19B (projection → 19B.3)

- **19B.3** — projection table `tenant_dashboard_snapshots`
- **19B.4** — `GET /api/v1/dashboard/snapshot`
- **19B.5** — pre-warm
- **19B.6** — remoção widgets legacy (D10)

Actualizado em `docs/roadmap.md`, `docs/features/dashboard.md`, `docs/architecture.md` e referências D10 em `dashboard.mdc` / `dashboard.md`.

## 8. Lista dos 11 testes adicionados

1. `ExecutiveDashboardObservabilityTest::test_record_l1_hit_incrementa_chave_diaria`
2. `ExecutiveDashboardObservabilityTest::test_record_l1_miss_l2_hit_l2_miss_e_invalidation_incrementam_buckets_distintos`
3. `ExecutiveDashboardObservabilityTest::test_snapshot_for_devolve_shape_e_hit_ratio_tres_casas`
4. `ExecutiveDashboardObservabilityTest::test_snapshot_com_counters_zero_hit_ratio_zero`
5. `ExecutiveDashboardObservabilityTest::test_counter_expira_apos_ttl_sete_dias`
6. `ExecutiveDashboardObservabilityTest::test_d15_chaves_canonicas_sem_tenant_nem_padroes_proibidos`
7. `L1L2InstrumentationTest::test_l1_primeira_leitura_miss_segunda_hit`
8. `L1L2InstrumentationTest::test_l2_primeira_leitura_miss_segunda_hit_e_none_sem_counters`
9. `CacheStatsCommandTest::test_comando_json_com_day_fixo_e_counters_conhecidos`
10. `CacheStatsCommandTest::test_comando_sem_day_imprime_tabela_e_day_invalido_falha`
11. `InvalidatorInstrumentationTest::test_invalidate_por_task_created_incrementa_invalidations_uma_vez`

## 9. Commit sugerido

```
feat(dashboard): observabilidade leve do cache executivo (19B.2)
```

**Motivo:** counters diários L1/L2/invalidações + comando de leitura; instrumentação mínima nos três call-sites; base para decisões futuras (TTL, projection, pre-warm) sem APM externo.

## 10. Riscos / limitações aceites

- **D15a:** `Cache::has()` → `remember`/`flexible` pode desviar ±1 % em corrida.
- **D17:** hits “stale” do `flexible` não distinguíveis sem wrap interno do Laravel.

## 11. Conclusão

A fase 19B.2 está **concluída** conforme a spec executável: código, testes (11), documentação e `result.md` preenchido. Próximo incremento lógico do roadmap: **19B.3** (projection table), quando houver requisito e capacidade de job/refresh.
