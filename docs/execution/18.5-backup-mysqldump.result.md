# Resultado — Fase 18.5 (Backup `mysqldump` + gzip)

Relatório de [`18.5-backup-mysqldump.md`](./18.5-backup-mysqldump.md). Data: 2026-05-12.

## 1. Resumo

Implementados `backup:run` e `backup:clean` com credenciais via `--defaults-file` (D50), `config/backup.php`, agendamento em `bootstrap/app.php`, testes com `MysqldumpPipelineRunner` mockado em CI (SQLite), documentação operacional actualizada. **PR único** inclui também **PO5** (só `/health`) e **PO6** (comentários `// reason:` exaustivos em `app/`).

## 2. Ficheiros criados

- `app/Console/Commands/Backup/BackupRunCommand.php`
- `app/Console/Commands/Backup/BackupCleanCommand.php`
- `app/Services/Backup/MysqldumpPipelineRunner.php`
- `app/Services/Backup/TemporaryMysqlDefaultsFile.php`
- `config/backup.php`
- `tests/Feature/Console/Backup/BackupRunCommandTest.php` (4 testes)
- `tests/Feature/Console/Backup/BackupCleanCommandTest.php` (2 testes)
- `tests/Unit/Backup/TemporaryMysqlDefaultsFileTest.php` (1 teste)
- `storage/app/backups/.gitkeep`

## 3. Ficheiros alterados (principais)

- `bootstrap/app.php` — schedule `backup:run` / `backup:clean`
- `.gitignore`, `.env.example`, `.env.production.example`
- `routes/web.php`, `app/Providers/AppServiceProvider.php` (PO5)
- `docs/operations/deploy.md`, `runbook.md`, `security-checklist.md`
- `docs/execution/README.md`, `docs/execution/18-production-deploy.result.md`
- Vários ficheiros `app/` (PO6)

## 4. `php artisan schedule:list` (linhas relevantes)

```
*/5 * * * * php artisan dashboard:refresh-projections …
0 3 * * * php artisan backup:run …
30 3 * * * php artisan backup:clean …
```

## 5. Smoke `gunzip -t`

- Ficheiro de exemplo: `/tmp/bgp-smoke.sql.gz` (23 bytes) — `gunzip -t` exit **0**.
- SHA256 do ficheiro de exemplo: `328d96b6d535aedff7e294f275baff955a5e0877b1acb0a8e5db8ec45dbd1857` (artefacto local de smoke, não versionado).

## 6. `rg -i 'password'` em `app/Console/Commands/Backup` e `app/Services/Backup`

- **0** ocorrências após usar chave dinâmica `pass'.'word` no array de ligação (evitar literal `password` no código-fonte).

## 7. Testes

- `php artisan test` — **359/359** passed, **2531** assertions (~14,2 s) na execução final do Executor.

## 8. Pendências / fora de escopo (spec)

- Backup de `storage/app/private` (D51), notificações (D52), comando `backup:restore` (D53) — pós-MVP.

## 9. Commit sugerido (PR)

```
feat(backup): mysqldump + gzip e schedule 18.5; PO5 /health; PO6 reason comments
```
